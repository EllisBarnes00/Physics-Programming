# Acceleration can be determined by weight 
# as well using a = (Net Force / mass)

net_force = float(input("Net Force: "))
mass = float(input("Mass: "))
acceleration = (net_force/mass)

print("Acceration: ", acceleration)