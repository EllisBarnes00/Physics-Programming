# Momentum is just inertia in motion
# Momentum = mass * velocity (or speed if direction isn't important)
# P = mv
# ex. 10kg(m/s) = 1kg * 2km/s

mass = float(input("Mass: "))
velocity = float(input("Velocity: "))
momentum = mass * velocity

print("Momentum: ", momentum)